import {
  View,
  Text,
  SafeAreaView,
  Image,
  StatusBar,
  FlatList,
} from "react-native";

import { COLORS, SIZES, FONTS, SHADOWS, assets } from "../constants";

import { CircleButton, RectButton, SubInfo } from "../components";

function Details() {
  return (
    <View>
      <Text>Details</Text>
    </View>
  );
}

export default Details;
